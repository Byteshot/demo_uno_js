export default class Carta
{
    id
    color
    numero
    figura
    tipoCarta
    constructor(id, color, numero, figura, tipoCarta)
    {
        this.id = id
        this.color = color
        this.numero = numero
        this.figura = figura
        this.tipoCarta = tipoCarta
    }
}
